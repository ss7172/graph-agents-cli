# Sourced by the GitHub workflows (which are copied verbatim, not rendered).
IMAGE_REPOSITORY={{cookiecutter.registry}}/{{cookiecutter.project_name}}
RELEASE_NAME={{cookiecutter.project_name}}
CHART_PATH=deployment/helm/{{cookiecutter.project_name}}
RUNTIME={{cookiecutter.runtime}}
CD={{cookiecutter.cd}}
# Where the workflows install graph-agents-cli from (uvx --from "$GRAPH_AGENTS_CLI_SPEC").
GRAPH_AGENTS_CLI_SPEC={{cookiecutter.cli_install_spec}}
