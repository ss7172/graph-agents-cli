# {{cookiecutter.project_name}}

Created by graph-agents-cli with `--deployment-target none`: no `deployment/`
directory, no CD workflows. Run it locally with `graph-agents-cli playground`
or `graph-agents-cli run "prompt"`; add a Kubernetes target later with
`graph-agents-cli scaffold enhance --deployment-target kubernetes`.
