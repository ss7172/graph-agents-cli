# Deployment target `none`

Marker for the target that ships no deployment files. `python/` holds only a
fallback README so a project built on the empty base has one; the langgraph
template overlays its own.
